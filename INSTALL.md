# 🚀 Установка и запуск Sber Congratulations Agent

## 📋 **Требования**

### **Минимальные:**
- Python 3.10 или выше
- Git
- Интернет соединение (для установки зависимостей)

### **Рекомендуемые:**
- 4 ГБ оперативной памяти
- 2 ГБ свободного места на диске
- Любая современная ОС (Windows 10+, macOS 10.15+, Ubuntu 20.04+)

---

## 📥 **ШАГ 1: Клонирование репозитория**

### **Для всех ОС:**
```bash
# Перейдите в папку для проектов (например, Documents или Desktop)
cd ~/Desktop  # для Mac/Linux
# или
cd %USERPROFILE%\Desktop  # для Windows

# Клонируйте репозиторий
git clone <URL-вашего-репозитория>
# Например:
# git clone https://github.com/ваш-логин/sber-congratulations-agent.git

# Перейдите в папку проекта
cd sber-congratulations-agent
```

**Если нет Git:**
1. Скачайте ZIP архив с GitHub
2. Распакуйте в удобную папку
3. Откройте терминал в этой папке

---

## 🐍 **ШАГ 2: Настройка Python окружения**

### **Windows:**
```cmd
# Проверьте версию Python
python --version

# Если Python не установлен, скачайте с python.org (версия 3.10+)
# Убедитесь, что отметили "Add Python to PATH" при установке

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
venv\Scripts\activate

# Если возникает ошибка выполнения скриптов, выполните в PowerShell от администратора:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# Затем повторите активацию
```

### **MacOS / Linux:**
```bash
# Проверьте версию Python
python3 --version

# Если Python 3.10+ не установлен:
# MacOS: brew install python@3.10
# Ubuntu/Debian: sudo apt install python3.10 python3.10-venv

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте окружение
source venv/bin/activate
```

**Проверка активации:** В начале строки терминала должно появиться `(venv)`

---

## 📦 **ШАГ 3: Установка зависимостей**

### **В активированном окружении выполните:**

```bash
# Обновите pip (менеджер пакетов Python)
pip install --upgrade pip

# Установите зависимости из requirements.txt
pip install -r requirements.txt

# Если возникает ошибка, попробуйте установить по отдельности:
pip install fastapi==0.104.1 uvicorn[standard]==0.24.0 sqlalchemy==2.0.23 pydantic-settings==2.1.0 python-dotenv==1.0.0 jinja2==3.1.2
```

### **Проверка установки:**
```bash
# Создайте тестовый скрипт
cat > check_install.py << EOF
import sys
print(f"Python версия: {sys.version}")

packages = ['fastapi', 'sqlalchemy', 'pydantic_settings', 'jinja2']
for pkg in packages:
    try:
        __import__(pkg)
        print(f"✅ {pkg}")
    except ImportError:
        print(f"❌ {pkg} не установлен")
EOF

# Запустите
python check_install.py

# Удалите тестовый файл
rm check_install.py
```

---

## 🔧 **ШАГ 4: Настройка переменных окружения**

### **1. Создайте файл `.env` в корне проекта:**

```bash
# Скопируйте пример конфигурации
cp .env.example .env
```

### **2. Отредактируйте `.env` файл:**

Откройте файл `.env` в любом текстовом редакторе и настройте:

```ini
# ===== БАЗОВЫЕ НАСТРОЙКИ =====
DEBUG=True
DATABASE_URL=sqlite:///./congratulations.db

# ===== API КЛЮЧИ (опционально) =====
# GIGACHAT_API_KEY=ваш_ключ_гигачат
# KANDINSKY_API_KEY=ваш_ключ_кандинский
# TELEGRAM_BOT_TOKEN=токен_бота

# ===== SMTP ДЛЯ ОТПРАВКИ EMAIL (опционально) =====
# Для тестирования можно использовать Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=ваш_email@gmail.com
# SMTP_PASSWORD=пароль_приложения  # Не пароль от почты!

# ===== НАСТРОЙКИ СИСТЕМЫ =====
BIRTHDAY_DAYS_AHEAD=7
DEFAULT_TONE=дружеский
USE_REAL_AI=False  # Пока оставьте False
```

**Важно:** Для демо достаточно оставить только `DEBUG=True` и `DATABASE_URL`, остальное можно закомментировать.

---

## 🗄️ **ШАГ 5: Инициализация базы данных**

```bash
# 1. Создайте таблицы в базе данных
python -c "
import sys
sys.path.append('.')
from src.core.database import create_tables
create_tables()
print('✅ Таблицы БД созданы')
"

# 2. Заполните базу тестовыми данными
python src/scripts/seed_database.py

# На вопрос "Очистить существующие данные?" ответьте 'y'
```

**Ожидаемый вывод:**
```
📦 ЗАПОЛНЕНИЕ БАЗЫ ДАННЫХ ТЕСТОВЫМИ ДАННЫМИ
============================================================
Очистить существующие данные и создать тестовые? (y/N): y
🧹 Очищаем базу данных...
✅ База данных очищена
👥 Создаем 20 тестовых клиентов...
✅ Создано 20 тестовых клиентов

📊 Статистика клиентов:
   - С ДР сегодня: X
   - С ДР завтра: Y
   - С ДР на этой неделе: Z

🎉 Создаем тестовую историю поздравлений...
✅ Создано N тестовых поздравлений
```

---

## 🚀 **ШАГ 6: Запуск сервера**

### **Вариант A: Базовый запуск**
```bash
# Запустите сервер
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
```

### **Вариант B: Запуск с логированием**
```bash
# Создайте скрипт запуска
cat > run.sh << 'EOF'
#!/bin/bash
echo "🚀 Запуск Sber Congratulations Agent..."
echo "📊 Режим: разработка"
echo "🌐 Адрес: http://localhost:8000"
echo "📚 Документация: http://localhost:8000/docs"
echo ""
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
EOF

# Сделайте скрипт исполняемым (Mac/Linux)
chmod +x run.sh

# Запустите
./run.sh
```

### **Вариант C: Для Windows (Powershell)**
```powershell
# Создайте скрипт запуска
@'
Write-Host "🚀 Запуск Sber Congratulations Agent..." -ForegroundColor Green
Write-Host "📊 Режим: разработка" -ForegroundColor Yellow
Write-Host "🌐 Адрес: http://localhost:8000" -ForegroundColor Cyan
Write-Host "📚 Документация: http://localhost:8000/docs" -ForegroundColor Cyan
Write-Host ""
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
'@ | Out-File -FilePath run.ps1 -Encoding UTF8

# Запустите
.\run.ps1
```

---

## ✅ **ШАГ 7: Проверка работоспособности**

### **1. Откройте браузер и перейдите по адресам:**

- **Главная страница:** http://localhost:8000/
- **Документация API (Swagger):** http://localhost:8000/docs
- **Альтернативная документация:** http://localhost:8000/redoc
- **Health check:** http://localhost:8000/health

### **2. Проверьте API через терминал:**

```bash
# Health check
curl http://localhost:8000/health

# Список клиентов
curl http://localhost:8000/api/v1/clients

# Дни рождения сегодня
curl http://localhost:8000/api/v1/clients/birthdays/today
```

### **3. Протестируйте генерацию поздравлений:**

```bash
# Получите ID любого клиента
curl -s http://localhost:8000/api/v1/clients | python -m json.tool | grep -A2 '"id"'

# Сгенерируйте поздравление (замените 1 на реальный ID клиента)
curl -X POST "http://localhost:8000/api/v1/congratulations/generate?client_id=1"
```

---

## 🐛 **ШАГ 8: Решение возможных проблем**

### **Проблема 1: Python не найден**
```
'python' не является внутренней или внешней командой
```
**Решение:**
- Убедитесь, что Python установлен: `python --version`
- На Windows добавьте Python в PATH
- На Mac/Linux используйте `python3`

### **Проблема 2: Ошибка импорта модулей**
```
ModuleNotFoundError: No module named 'fastapi'
```
**Решение:**
```bash
# Проверьте, что виртуальное окружение активировано
# (В начале строки должно быть (venv))

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### **Проблема 3: Порт 8000 занят**
```
Error: [Errno 48] Address already in use
```
**Решение:**
```bash
# Используйте другой порт
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8001

# Или освободите порт (Mac/Linux)
lsof -ti:8000 | xargs kill -9
```

### **Проблема 4: Ошибка с SQLite на Windows**
```
sqlite3.OperationalError: unable to open database file
```
**Решение:**
```bash
# Убедитесь, что у вас есть права на запись в папку проекта
# Запустите терминал от имени администратора
# Или измените DATABASE_URL в .env:
DATABASE_URL=sqlite:///C:/temp/congratulations.db
```

### **Проблема 5: Нет данных в базе**
```
Клиентов в базе: 0
```
**Решение:**
```bash
# Запустите скрипт заполнения БД
python src/scripts/seed_database.py
# Ответьте 'y' на запрос подтверждения
```

---

## 📱 **ШАГ 9: Работа с системой**

### **Быстрый старт:**

1. **Откройте** http://localhost:8000/
2. **Проверьте** статистику на главной странице
3. **Нажмите** "Проверить события" для поиска дней рождения
4. **Нажмите** "Сгенерировать" для любого клиента с ДР сегодня
5. **Нажмите** "Отправить" для отправки поздравления

### **Основные функции:**
- **Главная страница:** Статистика и быстрые действия
- **API документация:** Полный список доступных эндпоинтов
- **Тестовые данные:** 20 клиентов, у некоторых сегодня ДР

---

## 🛠 **ШАГ 10: Разработка и отладка**

### **Полезные команды:**
```bash
# Просмотр логов сервера
# (Логи отображаются в терминале, где запущен сервер)

# Проверка состояния базы данных
python -c "
from src.core.database import SessionLocal
from src.core.models import Client
db = SessionLocal()
count = db.query(Client).count()
print(f'Клиентов в базе: {count}')
db.close()
"

# Запуск тестов
python -c "
from src.modules.generator.text_generator import TextGenerator
from src.core.database import SessionLocal
db = SessionLocal()
generator = TextGenerator()
client = db.query(Client).first()
if client:
    result = generator.generate_for_client(db, client.id, 'birthday')
    print(f'✅ Генерация работает: {len(result['text'])} символов')
db.close()
"
```

### **Структура проекта для разработчиков:**
```
sber-congratulations-agent/
├── src/
│   ├── api/              # FastAPI приложение
│   ├── core/             # Ядро (БД, модели, конфиг)
│   ├── modules/          # Модули системы
│   └── frontend/         # Веб-интерфейс
├── data/                 # Тестовые данные
├── logs/                 # Логи (создаются автоматически)
└── congratulations.db    # База данных SQLite
```

---

## 🔄 **ШАГ 11: Обновление кода**

### **Получение обновлений:**
```bash
# Сохраните свои изменения
git stash

# Получите обновления
git pull origin main

# Верните свои изменения
git stash pop

# Установите новые зависимости (если есть)
pip install -r requirements.txt
```

### **Сброс к чистой версии:**
```bash
# Осторожно! Удалит все локальные изменения
git fetch origin
git reset --hard origin/main
```

---

## 📞 **Получение помощи**

### **Если что-то не работает:**
1. **Проверьте** все шаги установки
2. **Убедитесь**, что виртуальное окружение активировано
3. **Проверьте** наличие файла `.env`
4. **Посмотрите** ошибки в терминале

### **Для быстрой помощи:**
1. Сделайте скриншот ошибки
2. Покажите вывод команды `python --version`
3. Покажите содержимое файла `.env` (без паролей)
4. Опишите, на каком шаге возникла проблема

---

## ✅ **ПРОВЕРОЧНЫЙ ЛИСТ**

Перед тем как просить о помощи, проверьте:

- [ ] Python 3.10+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Все зависимости установлены
- [ ] Файл `.env` существует в корне проекта
- [ ] База данных заполнена тестовыми данными
- [ ] Сервер запущен на порту 8000
- [ ] В терминале нет ошибок

---

## 🎉 **ГОТОВО!**

Ваша система должна быть доступна по адресу:  
**http://localhost:8000**

Для разработки используйте:
- **Swagger UI:** http://localhost:8000/docs
- **Исходный код:** в папке `src/`
- **Базу данных:** `congratulations.db` (можно открыть в DB Browser for SQLite)

**Удачной разработки!** 🚀